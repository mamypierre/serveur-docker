````
build force run this when you change something dockerfile => sudo docker-compose up -d --build  php_8 
update run this when you change change docker-compose=> sudo docker-compose up -d   php_8


to run docker not admin
https://docs.docker.com/engine/security/rootless/


sudo service docker status
````